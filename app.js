var assert = require('assert');

console.log('test')

describe('Array Test', () => {
    describe('indexOf() Method', () => {
        it('return -a when null', () => {
			assert.equal(-1, [1,2,3].indexOf(0));
			assert.equal(-1, [1,2,3].indexOf(0));
			setInterval(() => {
				
			}, 1000);
		});
	});
});	